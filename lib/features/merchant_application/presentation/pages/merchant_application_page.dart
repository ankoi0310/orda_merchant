import 'package:flutter/material.dart';

class MerchantApplicationPage extends StatelessWidget {
  const MerchantApplicationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(child: Text('Đổ danh sách và nút đăng ký')),
    );
  }
}
